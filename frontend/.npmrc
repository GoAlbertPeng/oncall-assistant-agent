# Prevent hang in Docker build
loglevel=verbose
progress=true
fetch-retries=3
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000
timeout=120000

# China mirror (optional, uncomment if npm is slow)
registry=https://registry.npmmirror.com
